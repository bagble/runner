# === REQUIRED (Change before running in production) ===
# `SALT`: must be at least 32 characters. Used for HMAC/signing.
# Example: `openssl rand -hex 32`
SALT='super-secret-salt-at-least-32-characters-long'

# `SERVER_KEY`: secret key used for server-to-server auth. Replace with a long random value.
SERVER_KEY='super-secret-server-key'

# Optional rate-limit bypass lists (comma-separated)
RATELIMIT_BYPASS_IPS=""
RATELIMIT_BYPASS_USERIDS="1"

# PostgreSQL DB 설정
POSTGRESQL_HOST=postgres
POSTGRESQL_PORT=5432
# === DATABASE (Change for your environment) ===
# `POSTGRESQL_USER`: DB username
POSTGRESQL_USER=your_db_user
# `POSTGRESQL_PASSWORD`: DB password (REQUIRED if not using trust)
POSTGRESQL_PASSWORD=your_db_password
# `POSTGRESQL_NAME`: database name
POSTGRESQL_NAME=your_db_name
# `POSTGRESQL_SSL`: set to 'disable' for local docker; use 'require' in production if supported
POSTGRESQL_SSL=disable

# Redis 설정
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_USERNAME=
# `REDIS_PASSWORD`: set if your Redis requires AUTH (recommended in production)
REDIS_PASSWORD=your_redis_password
# `REDIS_DB`: integer DB index (default 0)
REDIS_DB=0
REDIS_TLS=false

# Server settings
# `HOST`/`PORT`: server listen address and port
HOST=0.0.0.0
PORT=8000
# `ORIGIN`: application origin used for CSRF/trusted origins
ORIGIN="http://localhost:8000"
CLOUDFLARED_TUNNEL=false
ALLOW_TEST_PAGE=true
SYS_LOG=true
SYS_LOG_STDOUT=true
SYS_LOG_LOCATION=./logs

# Email (optional): fill these only if you want email features (recovery/confirmations)
SMTP_HOST=
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=""
SMTP_SECURITY=force_tls
SMTP_PORT=

# === NOTES ===
# - Copy this file to `.env` and DO NOT commit `.env` to git.
# - Generate `SALT` and `SERVER_KEY` with a secure random generator (openssl, pwgen, etc.).
# - For Docker local development, the defaults (postgres/redis hosts) usually work.
# - In production, set proper passwords, enable Postgres SSL if required, and secure Redis with a password or network rules.
